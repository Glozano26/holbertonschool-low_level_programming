#include <stdio.h>
/**
 * main - entry point
 * @argc: lenght string
 * @argv: array
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	if (argc >= 0)
	{
		printf("%s", argv[0]);
	}
	return (0);
}

