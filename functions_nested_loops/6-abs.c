#include "main.h"
/**
 * _abs -  computes the absolute value of an integer.
 * @int: prueba
 * @r: parameter
 * Return: integer
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}
}
