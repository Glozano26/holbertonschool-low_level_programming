#include "main.h"
/**
 * _abs -  computes the absolute value of an integer.
 * @int: prueba
 * @r: parameter
 * Return: Always 0.
 */
int _abs(int r)
{
	int x, r;

	x = _abs(r);
	_putchar(x);

	return (0);	
}
