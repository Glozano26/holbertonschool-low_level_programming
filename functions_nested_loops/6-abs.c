#include "main.h"
/**
 * _abs -  computes the absolute value of an integer.
 * @int: prueba
 * @r: parameter
 * Return: Always 0.
 */
int _abs(int r)
{
	int r;

	_abs(r);
	_putchar(r);

	return (0);
}
