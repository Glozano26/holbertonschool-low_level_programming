#include "main.h"

/**
 * char *_strcpy - a function that copies the string pointed to by src
 * @dest: copy to
 * @src: copy from
 *
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	while (src[i++])
	{
		a++;

		for (i = 0; i <= a; i++)
		{
			dest[i] = src[i];
		}
		*dest = '\0';
	return (dest);
	}
}
